# Internal Service Configuration
# WARNING: This file contains sensitive information
# Access restricted to internal network only

service.name=SSRF-Internal-Secret-Service
service.env=production
database.host=10.0.0.5
database.port=3306
database.name=internal_secrets
database.user=admin
database.password=Sup3rS3cretP@ss!
admin.panel=http://10.0.0.5:8080/admin

# [SSRF_SECRET_FLAG_MARKER]
# The flag is managed by the challenge system.
# Accessing this file via file:// protocol triggers SSRF detection.
